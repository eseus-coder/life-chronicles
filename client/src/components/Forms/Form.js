import React from "react";
import useStyles from './styles';

const Form = () => {

    const classes = useStyles();
    
    return (
        <div>
            <h1>Form Component</h1>
        </div>
    );
};

export default Form;