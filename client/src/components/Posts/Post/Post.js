import React from "react";
import useStyles from './styles';

const Post = () => {

    const classes = useStyles();
    
    return (
        <div>
            <h1>Post Component(Inside Posts)</h1>
        </div>
    );
};

export default Post;